#Escreva uma função que recebe um vetor de números e escreva no console quais números aparecem mais de uma 
#vez e a quantidade de repetições. Exemplo:

def repeticao (list):

    val = []
    rep = []

    for i in list:
        if rep in num:
            rep.append(num)
        else:
            rep.add(num)
print(f'Valores = {val}')
print(f'Repetidos = {rep}')